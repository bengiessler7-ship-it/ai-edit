import { NextResponse } from "next/server"; import { v4 as uuid } from "uuid"; import { saveProject } from "@/lib/projectStore"; import { Project } from "@/lib/types";
export async function POST(){ const p:Project={id:uuid(),videoSourceType:"upload",videoUrl:null,uploadedVideoPath:null,uploadedMusicPath:null,editDescription:"",targetDuration:15,outputFormat:"9:16",resolution:"1080p",enhanceQuality:false,enhancementOptions:{upscale:false,sharpen:false,stabilize:false,denoise:false,colorBoost:false,fpsBoost:false},status:"created",progress:0,currentStep:"Projekt erstellt",editPlan:null,outputVideoPath:null,error:null,createdAt:new Date().toISOString()}; saveProject(p); return NextResponse.json(p); }
